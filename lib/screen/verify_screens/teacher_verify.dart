import 'package:flutter/material.dart';

class TeacherVerifyPage extends StatelessWidget {
  const TeacherVerifyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}